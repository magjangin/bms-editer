# BMS Editor - 확인이 필요한 알려진 문제 (Known Issues)

이 문서는 코드를 훑어 찾아낸 **의심 동작과 확정 버그**를 모아둔 것입니다.
실제로 써보면서 하나씩 확인하고, 확인이 끝난 항목은 아래 [확인 기록](#-확인-기록)에 남깁니다.

* 기준: 2026-09-22 전체 점검 · 2026-09-23 실사용 관점 점검 (코드는 `55a0e0b` 기준. 찾은 것은 고치지 않고 목록으로만 남겼습니다)
  * 작업이 날아가거나 조용히 틀리는 자리 → [🧨 실사용에서 터지는 자리](#-실사용에서-터지는-자리-2026-09-23-점검)
  * 코드를 읽다 찾은 의심 동작 → [🔍 2026-09-22 점검에서 찾은 것](#-2026-09-22-점검에서-찾은-것-확인-필요)
* 심각도 표기
  * 🔴 **재현 완료** — 테스트 코드로 실제 재현했습니다. 데이터가 손상됩니다.
  * 🟠 **코드상 확실** — 코드를 읽어 확인했습니다. 눌러보면 바로 드러납니다.
  * 🟡 **확인 필요** — 의심되는 동작입니다. 실사용 확인이 필요합니다.

> ✅ **해결된 핵심 과제**
> * **A. 조건 블록(`#RANDOM`/`#IF`/`#SWITCH`)**: 갈래(`BranchId`)와 원래 줄 순서를 추적해 갈래별로 나눠 저장합니다. (2026-09-22 점검에서 줄 배치에 따라 저장 순서가 섞이는 경우를 찾았습니다 → [A](#-a-조건-블록-지원-해결-완료-))
> * **B. 키음 믹싱 재생**: WAV (8/16/24/32bit PCM, IEEE Float, Extensible) 및 OGG 사전 디코딩(`WavDecoder`), `KeySoundPlayer` 다중 채널(`waveOut`) 믹싱 재생 (화음 및 연속 발음 지원). 지연은 [아래 🟡 항목](#-키음이-배경음보다-늦게-울릴-수-있다).
> * **D. 렌더링 할당 줄이기**: 마디·레인 라벨(`FormattedText`) 캐시, `Notes` 배열 캐시, `NoteStats` 단일 순회. 뷰포트 컬링은 시도했다가 **렌더 무결성 문제로 되돌렸습니다**(`10e1937`) → [D](#-d-렌더링-비용).
> * **F. 🎛️ 컨트롤 패널**: 📊 통계 창(보기 전용)은 그대로 두고, 같은 집계를 물려받은 공구함을 툴바에 더했습니다.
> * **v0.1.4 게임 프로파일 & 홀드 자동 페어링**: JSON 프로파일 규칙으로 홀드·게이트·페어리 등의 짝을 읽어 격자에 몸통으로 그리고, 편집할 때마다 짝을 검사합니다([사양](../specifications/hold_pairing_spec.md)).
>
> 단위 테스트는 107개 → 114개 → 125개 → **306개**이고, 모두 통과합니다.
> 이 중 `RealGameChartTests` 는 PC 에 설치된 게임 차트가 있을 때만 실제로 검사하고, 없으면 한 줄만 남기고 지나갑니다.

---

## 🎯 남은 일 (권장 순서)

| 순위 | 항목 | 크기 | 왜 여기인가 |
|------|------|------|-------------|
| **1** | [S-3 자동 저장](#-실사용에서-터지는-자리-2026-09-23-점검) + [S-1 전역 예외 처리](#-실사용에서-터지는-자리-2026-09-23-점검) | 중간 | **한 곡에 네 시간짜리 작업을 지키는 가장 큰 안전장치입니다.** 지금은 앱이 예외 하나로 꺼지면 저장 안 한 것이 통째로 사라지고, 왜 꺼졌는지도 안 남습니다 |
| **2** | [C. Undo / Redo](#-c-undo--redo) | 큼 | 저장은 안전하지만 **편집 자체를 되돌릴 방법이 없습니다.** 잘못 지운 노트는 복구가 어렵고 재작업 부담이 큽니다 |
| **3** | [S-8 · S-9 재생 정지](#-조용히-틀리는-경로) | 작음 | 파일을 열어도 재생이 안 멈추고, 옛 노트가 새 차트의 키음으로 소리를 냅니다. **틀린 소리로 박자를 판단하게 됩니다.** 두 줄이면 됩니다 |
| **4** | [🟡 키음이 배경음보다 늦게 울릴 수 있다](#-키음이-배경음보다-늦게-울릴-수-있다) | 작음~중간 | 박자 맞추기를 귀로 할 때 기준이 흔들립니다. 먼저 실물로 재 봐야 합니다 |
| **5** | [S-10~S-12 조용히 틀리는 것들](#-조용히-틀리는-경로) · [S-13~S-16 시간 먹는 것들](#-시간을-먹는-자리) | 작음 | 없는 키음·추측한 경로에 표시가 없고, 창 제목이 전부 `hwa2.bms` 입니다. 하나하나는 작지만 매 세션 반복됩니다 |
| **6** | [🔍 2026-09-22 점검에서 찾은 것](#-2026-09-22-점검에서-찾은-것-확인-필요) | — | 코드를 읽고 찾은 의심 동작 목록입니다. **고치지 않았습니다.** 실사용에서 확인하고 고칠지 판단합니다 |
| **7** | [25-2. 비디오가 ScrollViewer 밖으로 삐져나옴](#-25-2-비디오가-scrollviewer-밖으로-삐져나온다) | 작음 | 🟡 오른쪽 패널 스크롤 시 WebView2 경계 잘림 처리 실물 확인 및 보정 필요 |
| **8** | 팔레트 역할 뱃지 ([사양 §8](../specifications/hold_pairing_spec.md#8-표시-rendering)) | 작음 | 번호와 역할이 반대인 프로파일(`00B`=시작/`00A`=끝)에서 뒤집어 찍는 실수를 **찍기 전에** 막습니다 |
| **9** | [드래그로 홀드 배치 (P4)](../specifications/hold_pairing_spec.md#13-드래그로-홀드-배치--입력-충돌을-먼저-풀어야-한다) | 중간 | 홀드 자동 감지는 완료되었으나, 배치 자체는 여전히 시작/끝 2번 클릭 필요. Undo/Redo 완료 후 착수 권장 |

---

## 🔧 A. 조건 블록 지원 (해결 완료 ✅)

* **처음 문제**: 조건 블록(`#RANDOM`/`#IF`/`#SWITCH`) 안의 건반 줄이 평범한 노트로 읽혀 저장 시 한 줄로 합쳐지거나 헤더 위치로 끌려 올라갔습니다.
* **1차 해결(`7ec07f1`)**: `BmsNote`·`BmsRawLine` 에 갈래(`BranchId`)와 원본 줄 순서를 추적하고, 라이터가 갈래별로 줄을 나눠 씁니다.
* **2026-09-22 점검에서 찾은 것 (미해결)**: 라이터가 제어 줄(`#IF` 등)을 "바로 앞 데이터 줄의 마디"에 매달아 다른 줄과 함께 **마디 순으로 다시 정렬**합니다.
  줄이 마디 순으로 적힌 단순한 차트는 괜찮지만, 아래 배치에서는 저장 결과의 줄 순서가 섞입니다(임시 프로그램으로 저장 결과를 확인했습니다).
  - 블록 뒤에 같은 마디의 무조건 줄(BGM·건반)이 있으면, 그 줄이 `#IF 1` 안으로 들어갑니다.
  - 블록을 파일 끝에 몰아 둔 차트(블록 안 마디 < 블록 앞 줄의 마디)는 `#IF` 가 닫히지 않은 순서로 저장됩니다.
  - 블록 안의 주석과 모르는 헤더(`#BMP` 등)는 헤더 영역으로 옮겨집니다.
  → 자세한 내용과 확인 방법: [점검 목록 1·2번](#-2026-09-22-점검에서-찾은-것-확인-필요)

---

## 🔊 B. 키음 다중 채널 믹싱 재생 (해결 완료 ✅)

* **이전 문제**: Win32 `PlaySound(SND_ASYNC)`를 사용하여 단일 채널로 재생되었기 때문에, 화음이나 빠른 연타 시 앞선 소리가 끊기고 채보 귀 검수가 불가능했습니다.
* **해결 구조**:
  - `WavDecoder`를 통해 WAV (8/16/24/32bit PCM, IEEE Float, `WAVE_FORMAT_EXTENSIBLE`) 및 OGG 파일을 44100Hz 16-bit Stereo PCM으로 백그라운드 사전 디코딩하여 메모리에 캐싱.
  - Win32 `waveOut` 네이티브 헤더(`WAVEHDR`) 포인터를 직접 제어하는 `KeySoundPlayer` 스트리밍 믹서를 구축하여 포화 가산(Saturation Clamping)으로 실시간 다중 채널(폴리포닉) 믹싱을 완벽 지원합니다.

---

## ↩️ C. Undo / Redo

가장 큰 빈 자리입니다. 저장은 이제 안전하지만(원자적 저장 + `.bak`), **편집 자체는 되돌릴 수 없습니다.**

특히 위험한 조합:
* 검색창의 [삭제]는 한 번에 수백 개를 지웁니다
* [번호 일괄 변경]도 마찬가지입니다
* 둘 다 확인창 없이 즉시 실행됩니다

**고칠 방향**: 노트 편집이 전부 `MainWindowViewModel` 의 몇 개 메서드를 지나가므로
(`PlaceNote`·`RemoveNote`·`DeleteNotes`·`MoveSelectedNotes`·`ReplaceWavKey`·`CopyNotesByMeasureOffset`),
그 자리에 명령 기록을 끼우면 됩니다.

---

## 📉 D. 렌더링 비용

`NoteGridControl` · `OggWaveformControl` 은 매 프레임 타임라인 **전체**를 그립니다.
5분·BPM 150·16분할이면 격자선만 약 3,000개 × 2컨트롤, 192분할이면 약 36,000개 × 2.
재생 타이머가 33ms마다 위치를 바꾸므로 초당 30번 반복됩니다.

보이는 구간만 그리는 뷰포트 컬링을 넣었다가(`8b44887`) 격자선·노트가 빠지는 렌더 무결성 문제가 생겨 **되돌렸습니다**(`10e1937`).
지금은 전체를 그리고, 화면 밖은 Skia 캔버스 클리핑이 잘라냅니다. 느려지는 실사용 사례가 나오면 다시 봅니다.

| 항목 | 상태 |
|------|------|
| 마디 라벨·레인 라벨 `FormattedText` | ✅ 캐시 (`_measureLabelCache`, `_laneHeaderFormattedTextCache`) |
| `Notes`·`SelectedNotes` 배열 복사 | ✅ 바뀔 때만 다시 만든다 |
| 온셋 마커 펜(최대 2만 개) | ✅ 알파값 121가지 캐시 |
| 노트 조회가 선형 탐색 | `PlaceNote` 의 `Find` 가 O(n) — 남음 |
| 통계·컨트롤 패널이 편집마다 전체 재집계 | 차트를 **열 때**는 고쳤지만(28번), 노트 하나 찍을 때마다 도는 것은 그대로 — 남음 |
| 렌더마다 새로 만드는 펜 | 파형 격자선 펜 3개(`OggWaveformControl.DrawBeatGrid`), 재생 커서·번쩍임 펜(`TimelineControlBase`) — 남음 |

---

## 🟡 키음이 배경음보다 늦게 울릴 수 있다

[`KeySoundPlayer`](../../bms%20editer/Services/KeySoundPlayer.cs) 는 40ms 버퍼 3개를 앞서 채워 두고 재생합니다.
새 키음은 **이미 대기열에 들어간 버퍼들 뒤**에 섞이므로 구조상 최대 약 80~120ms 늦게 나옵니다.
울릴 노트를 고르는 재생 타이머도 33ms 간격이라 그만큼 더 흔들립니다. 배경음(`OggAudioPlayer`)은 곡 전체를 한 버퍼로 넘기고
장치가 실제로 재생한 위치로 커서를 잡으므로 이 지연이 없습니다.

**확인 방법**: 클릭 트랙 음원에 같은 박마다 키음을 찍고 1.0x 로 재생해, 키음이 박보다 늦게 들리는지 들어 봅니다(녹음해서 파형으로 재면 확실합니다).

**고칠 방향**: 버퍼를 줄이거나(끊김과 맞바꿈), 대기 중인 버퍼 길이만큼 키음을 **미리** 예약해 두는 방식(재생 위치 + 지연만큼 앞의 노트를 미리 믹서에 넣기).
박자 가이드에는 "판단은 배속을 낮춘 상태에서" 라고 적어 두었습니다([beat_sync_workflow.md](../guides/beat_sync_workflow.md)).

---

## 🔍 2026-09-22 점검에서 찾은 것 (확인 필요)

문서와 코드를 대조하다가 코드를 읽고 찾은 의심 동작입니다. **고치지 않았습니다.** 직접 써 보면서 확인하고, 고칠지는 그때 판단합니다.
(한 번 고쳐서 `main` 에 올렸다가, 요청 범위 밖이라 `1d994c9` 에서 되돌렸습니다. 그때의 수정은 커밋 `78f2eb3` · `01ba25d` 에 남아 있어 참고할 수 있습니다.)

| # | 항목 | 코드상 동작 | 확인 방법 | 코드 위치 |
|:--:|:---|:---|:---|:---|
| 1 | 🔴 조건 블록 저장 순서 | 제어 줄이 "직전 데이터 줄의 마디"로 정렬돼, 블록 뒤 같은 마디의 무조건 줄이 `#IF` 안으로 들어가거나, 블록을 파일 끝에 둔 차트는 `#IF` 가 닫히지 않은 순서로 저장된다 (임시 프로그램으로 재현) | `#00101` 뒤에 `#RANDOM`/`#IF 1`/`#00211`/`#ENDIF`/`#ENDRANDOM`, 그 뒤에 `#00201` 이 오는 차트를 열었다 저장해 줄 순서를 본다 | `BmsWriter.Write` 의 데이터 줄 정렬, `BmsParser.Parse` 의 제어 줄 `Measure = currentMeasure` |
| 2 | 🔴 `#SWITCH` 뒤 노트의 갈래 | `#CASE`·`#DEF` 마다 갈래를 쌓기만 하고 닫지 않아, `#ENDSW` 뒤의 평범한 노트가 마지막 CASE 의 갈래 번호로 읽힌다 (임시 프로그램으로 재현). 홀드 짝은 갈래가 다르면 짝짓지 않는다 | CASE 가 둘 이상인 `#SWITCH` 뒤에 노트를 둔 차트를 연다 | `BmsParser.Parse` 의 제어 줄 처리 |
| 3 | 🟡 조건 블록 안의 헤더 | 1단계에서 읽어 가는 헤더(`#WAV`·`#BPM`·`#TITLE` 등)는 저장할 때 헤더 영역에 새로 써서, 블록 안에 있던 것도 블록 밖(무조건)으로 옮겨진다. 같은 키가 두 번이면 마지막 값만 남는다 | 갈래마다 다른 `#WAV01` 을 둔 차트를 열었다 저장한다 | `BmsParser.IsConsumedHeader`, `BmsWriter.Write` |
| 4 | 🟠 격자 스크럽의 오프셋 | 파형을 누를 때는 오프셋을 빼는데, 격자에서 가운데 버튼으로 끌 때와 재생바 자동 스크롤은 오프셋을 빼지 않는다. 오프셋이 0 이 아니면 커서가 누른 자리에서 그만큼 떨어진다 | 오프셋을 +100ms 로 두고 격자를 가운데 버튼으로 끌어 커서 위치를 본다 | `MainWindow.Scrubbing.ScrubFromEditorSurface`, `MainWindow.Viewport.FollowPlaybackCursorIfNeeded` |
| 5 | 🟠 음원 없는 폴더 열기 | 폴더 열기에서 영상이 없으면 영상은 떼지만, 음원이 없으면 앞 곡 음원이 그대로 남는다 | OGG 가 있는 폴더를 연 뒤 OGG 없는 폴더를 연다 | `MainWindow.LoadFolderMediaAsync` |
| 6 | 🟠 `(G)`·`(D)` 표시 | 체크박스 이름에 "격자에 맞추기(G)" · "수직위치 고정(D)" 이라고 적혀 있지만 G·D 키에 연결된 동작이 없다 | 격자를 누른 뒤 G·D 를 눌러 본다 | `MainWindow.axaml`, `MainWindow.Input.HandleEditorKey` |
| 7 | 🟡 접근 불가 하위 폴더 | 키음 색인을 만들 때 차트 폴더를 `SearchOption.AllDirectories` 로 훑어서, 들어갈 수 없는 하위 폴더가 있으면 예외로 열기가 실패한다. 파일이 아주 많은 폴더에서는 열기가 느려진다 | 권한 없는 하위 폴더가 있는 폴더의 차트를 연다 | `BmsParser.BuildFileNameIndex` |
| 8 | 🟡 키음 미리 읽기 | 차트의 키음 표(`WavTable.Values`)를 그대로 백그라운드로 넘겨 훑는다. 그동안 키음을 추가·삭제하면 열거가 예외로 끊겨 미리 읽기가 조용히 멈출 수 있다 | 키음이 많은 차트를 연 직후 키음을 추가한다 | `MainWindowViewModel.LoadBms`, `KeySoundPlayer.PreloadAsync` |
| 9 | 🟠 테스트 문자열 | `AudioOffsetTests.cs` 113줄의 차트 문자열 줄바꿈이 PowerShell 식(`` `r`n ``)이라 C# 에서는 줄바꿈이 아니다. 차트 전체가 제목 한 줄로 읽힌다. 테스트는 통과하지만 의도한 차트를 검사하지 않는다 | 해당 줄을 본다 | `bms editer.Tests/AudioOffsetTests.cs` |
| 10 | 🟠 코드 주석 | 동작과 다른 주석: `BmsChart.HasConditionalBlocks`("이 표시를 보고 저장을 막는다" — 막는 곳 없음), `BmsParser.Patterns.ControlFlowRegex`("아직 해석하지 못한다"), `NoteGridControl` 머리 주석("편집은 다음 단계에서 확장"), `OggPeakLoader` 머리 주석("디코딩해서" — 지금은 디코딩된 PCM 을 받음) | — | 각 파일 |

---

## 🧨 실사용에서 터지는 자리 (2026-09-23 점검)

한 곡에 네 시간이 걸리는 도구라는 전제로, **작업이 날아가거나 조용히 틀리는 자리**만 따로 모았습니다.
문법이나 문서 불일치가 아니라 "쓰다 보면 실제로 당하는 것"들입니다. 아직 고치지 않았습니다.

### 🔴 작업이 날아가는 경로

| # | 증상 | 왜 | 확인 방법 | 고칠 방향 |
|:--:|:---|:---|:---|:---|
| S-1 | **앱이 예외 하나로 그대로 꺼진다.** 저장 안 한 작업이 통째로 사라진다 | 전역 예외 처리기(`Dispatcher.UIThread.UnhandledException`)가 없다. 클릭 핸들러가 `async void` 11곳(`MainWindow.FileIO.cs` 7 · `WavPaletteWindow` 1 · `OnWindowClosing` 1 · `VideoPreviewControl` 2)이라 그 안에서 샌 예외는 아무도 못 잡는다. 실제 후보: `FindBestFile` 의 `Directory.EnumerateFiles`·`new DirectoryInfo(...)`(`MainWindow.FileIO.cs:228`, try 없음), 파일 대화상자, WebView2 호출 | 권한 없는 폴더나 네트워크 드라이브를 [폴더 열기]로 고른다 | `Dispatcher.UIThread.UnhandledException` 에서 잡아 **"저장하고 종료할까요?"** 를 띄운다. 죽는 순간에도 작업을 건진다 |
| S-2 | **죽어도 이유가 안 남는다** | `Program.cs:21` 이 `crash.log` 를 exe 폴더에 쓴다. Program Files 아래면 이 쓰기도 실패해 원래 예외가 가려진다. 게다가 이 `try` 는 `StartWithClassicDesktopLifetime` 을 감싸서, UI 스레드에서 터진 예외는 여기까지 오지도 않는다 | 크래시 뒤 exe 폴더를 본다 | 로그를 `%LocalAppData%` 아래로. S-1 의 처리기에서 같은 파일에 쓴다 |
| S-3 | **자동 저장이 없다** | 저장은 사용자가 누를 때만 일어난다 | — | `%LocalAppData%` 아래 `autosave` 폴더에 1~3분마다 저장하고 최근 몇 개만 남긴다. 다음 실행 때 **복구 제안**. S-1·S-4·S-5 를 한 번에 덮는 가장 큰 안전장치 |
| S-4 | **저장을 두 번 하면 `.bak` 이 덮어써진다** | `SafeFileWriter` 는 `<파일>.bak` 하나만 유지한다. "실수로 지움 → 저장 → 어? → 다시 저장" 이면 복구본이 사라진다. 되돌리기도 자동 저장도 없어 이 시점에서 복구 수단이 0이 된다 | 저장을 두 번 하고 `.bak` 을 연다 | `.bak` 을 세대별로 남기거나 S-3 과 묶는다 |
| S-5 | **검색 창의 [삭제]·[번호 일괄 변경]이 확인 없이 즉시 실행된다** | `NoteSearchViewModel.DeleteMatches` · `ReplaceWavKey`. 조건에 맞는 수백 개가 한 번에 바뀐다(컨트롤 패널의 삭제만 확인창이 있다) | 조건을 넓게 잡고 [삭제]를 누른다 | 개수가 일정 수를 넘으면 확인창. 근본은 되돌리기([C](#-c-undo--redo)) |
| S-6 | **편집 모드에서는 좌클릭 한 번이 곧 배치다** | `NoteGridControl.OnPointerPressed`. 이미 노트가 있는 자리에 찍으면 **기존 노트의 키음을 조용히 덮어쓴다**(`PlaceNote`). 되돌릴 방법은 같은 자리 우클릭뿐 | 홀드 시작 노트 위에 다른 키음을 찍어 본다 | 되돌리기. 그 전까지는 덮어쓸 때 짝 검사기의 고아 경고로 알아채는 수밖에 없다 |
| S-7 | 🟡 **종료할 때 크래시할 수 있다** | `KeySoundPlayer.Dispose` 가 믹서 스레드를 300ms 만 기다린 뒤 네이티브 버퍼를 해제한다. 그 스레드가 아직 버퍼를 쓰고 있으면 접근 위반이 난다. OGG 를 읽는 도중 창을 닫으면 새 플레이어가 정리되지 않고 남는 자리도 있다(`LoadOggAsync`) | 키음이 잔뜩 울리는 중에 창을 닫는다 | 종료 신호를 주고 스레드가 끝날 때까지 기다린 뒤 해제 |

### 🟠 조용히 틀리는 경로

| # | 증상 | 왜 | 확인 방법 | 고칠 방향 |
|:--:|:---|:---|:---|:---|
| S-8 | **파일을 열어도 재생이 안 멈춘다.** 옛 음원이 계속 나오고, 옛 노트의 번호가 **새 차트의 키음 표**에서 소리를 찾아 엉뚱한 소리가 난다 | `LoadBms` → `ResetDocumentState` 경로에 `StopPlayback` 이 없다. `_playbackNotes` 는 `PlayFrom` 시점의 배열이라 옛 노트를 들고 있고, `PlayWavSound` 는 `Chart.WavTable`(새 차트)에서 번호를 찾는다. 폴더 열기는 뒤이어 `LoadOggAsync` 가 돌며 우연히 멈춘다 → **`Ctrl+O` 로 차트만 열 때만** 생긴다 | 재생 중 `Ctrl+O` 로 다른 곡을 연다 | `LoadBms` 앞에서 정지 |
| S-9 | **정지해도 길게 울리는 키음은 계속 난다** | `StopPlayback` 은 배경음만 멈춘다. `KeySoundPlayer.StopAll` 은 앱 코드에서 한 번도 부르지 않는다(호출은 `KeySoundPlayerTests.cs:147` 뿐) | 긴 키음이 울릴 때 Space 로 멈춘다 | 정지·문서 전환 때 `StopAll` |
| S-10 | **키음 파일이 없어도 화면에 표시가 없다** | 경로가 틀리면 `WavDecoder` 가 `null` 을 주고 `PlaySound` 폴백도 실패해 그냥 무음이다. 팔레트·통계 어디에도 "없음" 표시가 없다 | `#WAV` 경로를 일부러 틀리게 적고 연다 | 목록에 `⚠` 표시, 열 때 "찾지 못한 키음 N개" 한 줄 |
| S-11 | **경로를 추측해 찾은 키음도 표시가 없다** | 적힌 자리에 파일이 없으면 하위 폴더에서 같은 이름을 찾아 붙인다(`IsPathGuessed`). 옛 백업 폴더가 남아 있으면 **에디터는 옛 소리를, 게임은 원문 경로의 소리를** 낸다. 저장 파일은 원문을 지키므로 파일은 안전하지만 귀 검수 결과가 게임과 달라진다 | 하위 폴더에 같은 이름의 옛 wav 를 두고 연다 | 추측한 항목에 표시 |
| S-12 | **`#WAV` 에 없는 번호를 쓰는 노트**가 격자에서는 평범해 보인다 | 통계·컨트롤 패널 목록에만 "(등록되지 않은 번호)" 로 나온다. 게임에서는 무음 노트가 된다 | 등록되지 않은 번호로 번호 일괄 변경을 해 본다 | 격자에서도 구분되게 그리기 |

### 🕒 시간을 먹는 자리

| # | 증상 | 왜 | 고칠 방향 |
|:--:|:---|:---|:---|
| S-13 | **창 제목이 전부 `hwa2.bms` 다** | 이 PC 의 게임 차트 30개가 전부 파일명이 `hwa2.bms` 이고 폴더만 다르다. 제목 표시줄은 파일명만 보여준다(`WindowTitle`) | 제목에 `곡 폴더명 / 파일명` 또는 `#TITLE` 을 함께 |
| S-14 | **파일 대화상자가 최근 폴더를 기억하지 않는다** | `SuggestedStartLocation` 을 주지 않아 매번 기본 위치에서 시작한다. 실제 경로는 `steamapps/common/<게임>/hwa/<곡>/` 로 깊다. 드래그앤드롭도 없다 | 마지막 폴더 기억, 창에 파일 끌어다 놓기 |
| S-15 | **볼륨 조절이 없다** | 배경음과 키음의 균형을 맞출 수단이 없다. 키음이 크면 배경음의 킥이 묻혀 귀 검수가 어렵다 | 배경음·키음 각각 볼륨 슬라이더 |
| S-16 | **폴더 열기는 차트를 조용히 하나 고른다** | 파일명이 폴더명과 같은 것을 먼저 찾고, 없으면 **이름 순 첫 번째**를 연다(`FindBestFile`). 지금은 곡마다 차트가 하나뿐이라 무해하지만, 난이도별 파일을 늘리면 다른 차트를 열고도 모른다 | 차트가 둘 이상이면 고르게 하기 |

---

## 🟡 25-2. 비디오가 ScrollViewer 밖으로 삐져나온다

[VideoPreviewControl.cs](../../bms%20editer/Views/Controls/VideoPreviewControl.cs) 는
네이티브 창(HWND)이라 Avalonia 의 `ClipToBounds` 가 먹지 않습니다.
`_controller.Bounds` 를 좌표만 보고 잡는데, 오른쪽 패널을 스크롤하면 원점이 패널 밖으로 나갑니다.
툴바 위로 비디오가 삐져나올 수 있습니다.

**확인 방법**: 비디오를 띄운 채 오른쪽 패널을 위아래로 스크롤합니다.

**고칠 방향**: 부모의 보이는 영역과 교차한 사각형으로 `Bounds` 를 잘라 넣고,
완전히 벗어나면 `IsVisible = false` 로 둡니다.

---

## 🎛️ 되살린 것: 집계에서 키음으로 노트 찾기

한때 통계 창에 있었다가 **걷어냈던** 기능입니다. 이번에 **🎛️ 컨트롤 패널**이라는 별도 창으로 다시 만들어 넣었습니다.
(`ControlPanelViewModel` · `ControlPanelWindow`)

### 왜 접었었나

만드는 동안 세 번 헛돌았고, 매번 원인이 달랐습니다.

| 시도 | 무엇이 문제였나 |
|------|----------------|
| 1 | 명령을 `{Binding $parent[ItemsControl].((vm:NoteStatsViewModel)DataContext).…}` 로 끌어왔는데, 컴파일된 바인딩에서 **조용히 실패**했습니다. `Command` 가 null 인 버튼이라 눌러도 아무 일이 없었고, 빌드도 예외도 멀쩡했습니다 |
| 2 | 버튼 스타일(`Background`·`BorderBrush`)이 **Fluent 테마의 Button 템플릿에 가려** 화면에 안 나타났습니다. 누를 수 있다는 표시가 없으니 누를 생각을 할 이유도 없었습니다 |
| 3 | `ListBox` 로 바꿔 동작은 했지만, 고른 노트가 화면 밖이면 여전히 눌렸는지 알기 어려웠습니다. 격자를 그리로 스크롤하고 안내문을 붙이며 덧대다 보니 통계 창의 몫이 아닌 일이 늘었습니다 |

### 이번에는 무엇을 다르게 했나

| 예전에 걸린 것 | 이번 방식 |
|----------------|-----------|
| 목록 항목 안에서 조상 바인딩으로 명령을 끌어옴 | **목록 항목에는 명령을 걸지 않습니다.** 고른 줄은 `SelectedItem` 으로만 받고(`SelectedLaneStat`·`SelectedWavStat`), 누르는 것은 전부 목록 **밖**의 버튼입니다. 창의 `DataContext` 가 곧 뷰모델이라 바인딩이 한 칸도 건너뛰지 않습니다 |
| 버튼이 테마 템플릿에 가려 안 보임 | 항목 안에 버튼을 두지 않으니 가려질 자리 자체가 없습니다. 고른 줄은 검색 창·팔레트와 같은 파랑(`#3B6FD4`)으로 강조합니다 |
| 고른 노트가 화면 밖이면 눌렸는지 알 수 없음 | 선택을 `NoteSelectionSource.Search` 로 알려 **그 자리로 스크롤**시키고(`ScrollToRatioRequested`), 결과를 창 아래 한 줄(`StatusMessage`)로 말해 줍니다 |
| 뷰모델은 멀쩡한데 화면에서 끊어짐 | 창을 실제로 띄워 **작업 버튼 6개에 `Command` 가 붙어 있는지** 확인하는 테스트를 넣었습니다(`WindowSmokeTests`) |

검색 창과 역할이 겹치지 않느냐는 판단은 이렇게 갈랐습니다.
**"이 번호가 몇 개 있나"에서 곧장 손대는 것**은 컨트롤 패널이 맡고,
**"조건을 조합해 찾는 것"**(마디 범위 · 노트 종류 · 여러 레인)은 그대로 검색 창의 몫입니다.
컨트롤 패널의 일괄 작업은 언제나 **고른 줄 하나**를 대상으로 합니다.

> 📊 통계 창은 **보기 전용으로 남겨 두었습니다.** 숫자만 볼 때 잘못 눌러 수백 개가 바뀌는 일이 없고,
> 집계 규칙은 `NoteStatsViewModel` 한 곳뿐이라 두 창의 숫자가 갈라지지 않습니다.

> 되돌리기가 아직 없으므로([C](#-c-undo--redo)), 컨트롤 패널의 **일괄 삭제만은 먼저 물어봅니다.**
> 확인 창을 띄울 길이 없으면(`ConfirmAsync` 가 없으면) 아무것도 지우지 않습니다.

### 🅿️ 나중에 고려: 패널 맨 위의 열(레인) 선택 칸

"레인별 노트 개수 + [레인 노트 선택]" 칸은 **모양을 다시 볼 자리로 남겨 둡니다.**
지금은 한 번에 레인 하나만 고를 수 있어서, 여러 열을 묶어 고르는 일은 검색 창의 열 토글이 더 낫습니다.
게다가 편집 대상 레인이 `16,11,12,13,14,15,18` 로 고정돼 있어 열 개수 자체가 바뀔 여지가 있습니다.
**어떤 모양이어야 하는지는 그때 정합니다.** 그 전까지는 지금 형태로 둡니다.

같이 볼 것: 다중 선택(여러 레인 한꺼번에), 1P/2P 묶음 표시, 검색 창의 열 조건과 역할 나누기.

---

## ✅ 확인 기록

코드와 테스트로는 확인했지만 **실물로 눌러봐야 확실한** 것들입니다.
고친 항목 중 화면·소리·입력이 걸린 것만 남겼습니다.

| # | 항목 | 무엇을 확인하나 | 결과 | 확인일 |
|---|------|----------------|------|--------|
| 27 | 온셋 마커 위치 | 마커가 실제 타격음과 맞는지 (곡 끝에서 12~15ms 당겨져 있었음) | | |
| 2 | 격자에 맞추기 끄기 | 체크를 끄고 격자 사이에 찍으면 그 자리에 남는지 | | |
| 3 | 수직위치 고정 | 켠 채 방향키로 시간축 이동이 막히고 레인 이동은 되는지 | | |
| 4 | 잇단음 이동 | 12분할 3잇단음을 16분할에서 옮겨도 잇단음이 유지되는지 | | |
| 7-1 | 창 단축키 | 팔레트를 만진 뒤에도 Delete·방향키·Space 가 먹는지 | | |
| 13 | 창 닫기 확인 | 고친 뒤 X를 누르면 3지선다가 뜨고, 안 고쳤으면 그냥 닫히는지 | | |
| 14·35 | 박자·BPM 변화 | 그런 차트에서 격자·노트·키음이 소리와 맞는지 | | |
| 24 | 스크러빙 | 가운데 버튼 드래그가 딸깍거리지 않고, 뗄 때 그 자리부터 재생되는지 | | |
| 25-2 | 비디오 잘림 | 오른쪽 패널을 스크롤할 때 비디오가 삐져나오는지 | | |
| 29 | 재생/정지 | 메뉴 [재생] 과 Space 가 동작하는지 (툴바 버튼은 `ea93c56` 에서 뺐습니다) | | |
| 31 | 가로 줌 | 슬라이더를 움직이면 격자 폭이 곧바로 따라오는지 | | |
| 32 | 파형 스크럽 좌표 | 창을 크게 키운 뒤 파형의 특정 지점을 눌러 그 자리 소리가 나는지 | | |
| 33 | OGG 로딩 | 5분짜리 곡을 열 때 창이 멈추지 않는지 | | |
| 36 | Ctrl/Shift 선택 | 편집 모드에서 Ctrl+드래그로 선택이 되고, 기존 선택에 더해지는지 | | |
| 37 | 폴더 열기 | BMS 없는 폴더를 고르면 아무것도 안 바뀌는지 | | |
| 38 | 컨트롤 패널 일괄 선택 | 🎛️ 에서 레인/키음 줄을 고르고 [선택]을 누르면 격자가 그 자리로 스크롤하고 노트가 빨갛게 잡히는지 | | |
| 39 | 컨트롤 패널 미리듣기 | 키음 줄을 누를 때 소리가 나고, 편집 중 다시 집계될 때는 저절로 울리지 않는지 | | |
| 40 | 키음 지연 | [🟡 항목](#-키음이-배경음보다-늦게-울릴-수-있다)의 확인 방법 | | |

---

## 🗒️ 이미 고쳐진 것

| 커밋 | 항목 | 내용 |
|------|------|------|
| `f86a58c` | — | 파형 피크 버킷 절삭으로 시간축이 누적해서 어긋나던 문제 (2분 지점 53ms) |
| `15a867f` | — | 저장할 때 BGM·마디 길이·BPM 변화·확장 헤더 등이 사라지던 문제 (36줄 중 23줄 소실 → 1줄) |
| `e19c25b` | — | 재생 커서가 벽시계 기준이라 실제 소리보다 앞서 나가던 문제 (약 12ms + 드리프트) |
| `80c26b6` | — | **"테스트 프로젝트 없음"** — 파서·라이터 왕복과 격자선 계산 테스트가 생겼습니다 |
| `3a026fe` | 10 | 새 문서가 `#PLAYER 2`(Couple)로 저장되던 문제 |
| `3a026fe` | — | 격자선 계산이 네 벌로 흩어져 있던 문제 → `TimelineControlBase.EnumerateGridLines` 한 곳으로 |
| `3a026fe` | — | 보조 창들이 구독 해제 절차를 각자 복사해 갖고 있던 문제 → `OwnerObservingViewModel` 로 |
| `6e8fcf8` | **20** | 저장이 원자적이지 않던 문제 → `SafeFileWriter`. 임시 파일에 다 쓴 뒤 바꿔치기하고 `.bak` 을 남깁니다 |
| `6e8fcf8` | **8·9·19** | 인코딩 세 건 → 바이트로 한 번만 읽고 BOM → UTF-8(엄격) → CP932/CP949 순으로 감지. `#WAV` 파일명이 실제로 폴더에 있는지를 1순위 증거로 씁니다. 읽어낸 인코딩은 저장까지 이어집니다 |
| `6e8fcf8` | **11·1** | 3자리 키 두 건 → `keyWidth` 를 테이블과 노트 키 양쪽에서 잡고, `GetNextWavKey` 도 차트 자릿수에 맞춥니다 |
| `6e8fcf8` | **16** | `#WAV` 경로가 추측 결과로 덮어써지던 문제 → 추측한 경우에만 원문으로 되돌려 씁니다 |
| `6e8fcf8` | **18** | `#RANDOM`/`#IF` 차트 붕괴 → **저장 차단**으로 손상은 0. 해석은 [A](#-a-조건-블록-지원-해결-완료-)에서 했습니다 |
| `c910519` | **27** | 온셋 마커만 `i/(count-1)` 을 써서 곡 끝에서 12~15ms 앞당겨지던 문제. 규칙을 `OggPeakLoader.GetBucketRatio` 한 곳으로 |
| `c910519` | **21** | 재생 중 예외가 앱을 그대로 죽이던 문제 |
| `c910519` | **22** | OGG 로드 실패가 잘 물려 있던 음원까지 지우던 문제 |
| `c910519` | **24** | 스크러빙 중 마우스가 움직일 때마다 오디오 장치를 다시 열던 문제. 드래그 중엔 커서만, 뗄 때 한 번만 |
| `c910519` | **29** | 재생·정지 버튼이 없던 문제 (명령은 있는데 누를 곳이 없었음) |
| `c910519` | **7-1** | Delete·방향키가 격자에 포커스가 있을 때만 먹던 문제. Ctrl+S/O/N·Esc·Space 도 함께 |
| `c910519` | **37** | 폴더에 BMS 가 없으면 옛 차트 위에 새 음원만 얹히던 문제 |
| `14b1fc8` | **13** | 창을 X로 닫으면 저장 안 한 작업이 경고 없이 사라지던 문제 |
| `14b1fc8` | **26-6** | `ConfirmWindow` 2지선다 한계 → 저장/저장 안 함/취소 3지선다 |
| `14b1fc8` | — | dirty 추적과 제목 표시줄(파일명 + `*`) 추가. 안 고쳤는데 확인창이 뜨던 것도 함께 |
| `b48cc23` | **12·7-2·30** | 마디 수의 주인이 없던 문제. 오디오·차트 중 큰 쪽을 바닥으로 삼고, 마디 수 입력 칸을 붙였습니다 |
| `b48cc23` | **4** | 방향키 이동이 현재 격자로 강제 스냅되던 문제 |
| `b48cc23` | **5·6** | 그룹 이동이 일부만 움직이거나 겹치던 문제 → 하나라도 못 가면 아무것도 옮기지 않습니다 |
| `b48cc23` | **23** | 키음을 지워도 그 번호를 쓰던 노트가 남던 문제 → 지우기 전에 확인 |
| `b48cc23` | **7-4** | `[키음 추가]` 가 조용히 실패하던 문제 |
| `f2a4ec5` | **14·35** | 박자 변경·BPM 변화를 아무도 읽지 않던 문제 → `ChartTimeline` 한 곳으로. 격자·노트·클릭·재생이 모두 여기에 물어봅니다 |
| `f2a4ec5` | **31** | 가로 줌이 `AffectsMeasure` 에 빠져 크기가 안 바뀌던 문제 |
| `7d02c36` | **2·3** | "격자에 맞추기"·"수직위치 고정" 체크박스가 아무 일도 안 하던 문제 |
| `7d02c36` | **15** | "롱"·"숨기기" 필터가 절대 안 걸리던 문제 → 해당 노트가 생기기 전까지 잠금 |
| `7d02c36` | **7-3** | 우클릭 삭제 허용오차가 배치보다 50배 넓던 문제 → 격자 반 칸 안에서 가장 가까운 하나 |
| `7d02c36` | **7-6** | 파일 안의 주석이 저장할 때 사라지던 문제 |
| `7d02c36` | **26-1** | 중복 `#WAV` 가 두 줄로 늘어나던 문제 |
| `7d02c36` | **26-2** | 원본에 없던 빈 헤더가 생기던 문제 |
| `7d02c36` | **26-3** | 마디 1000 이상 줄이 헤더 블록으로 올라가던 문제 |
| `7d02c36` | **26-4** | 검색창 키 자릿수가 창을 연 시점에 고정되던 문제 |
| `7d02c36` | **26-5** | 곡 제목의 파일명 금지문자로 저장 대화상자가 걸리던 문제 |
| `f33007e` | **33** | OGG를 두 번 통째로 디코딩하고 창이 멈추던 문제 → `OggDecoder` 로 한 번만, `Task.Run` 으로 |
| `f33007e` | **17** | 디코더가 `TotalSamples` 보다 많이 내보내면 로드가 통째로 실패하던 문제 |
| `f33007e` | **28** | 차트를 열 때 통계·팔레트가 키음 개수만큼 재집계하던 문제 (301회 → 2회) |
| `4106152` | **36·26-7** | 편집 모드에서 선택이 안 되고 Ctrl/Shift 다중 선택도 없던 문제 |
| `4106152` | **25-1** | 비디오에 초당 30번 IPC 를 보내던 문제 → 0.2초에 한 번 |
| `4106152` | **25-3** | 초기화 도중 떼어내면 WebView2 컨트롤러가 새던 문제 |
| `4106152` | **32** | 타임라인 길이 공식의 세 번째 갈래(`Bounds`)를 공식으로 통일 |
| `4106152` | **34** | README 가 실제 동작과 반대로 적혀 있던 문제 (믹싱 재생 주장 → 한계 명시) |
| `4106152` | **7-5** | 저장 성공 표시 → 제목 표시줄의 `*` 가 사라지는 것으로 대신합니다 |
| `4106152` | — | 빈 껍데기였던 메뉴를 실제 명령으로 채우고, 안 쓰이던 멤버와 `.gitignore` 오타 정리 |
| `d297291` | — | 렌더마다 새로 만들던 펜·브러시를 공유로 |
| `8b44887` | **D** | 뷰포트 컬링 및 렌더링/컬렉션 할당 최적화 (가시 영역 격자/노트/파형 렌더링, `Notes` 배열 할당 제거, `NoteStats` 단일 패스) |
| `594d4f5` | **B** | 키음 WAV/OGG PCM 사전 디코딩 및 다중 채널(`KeySoundPlayer`) 믹싱 재생 지원 (화음 및 연속 발음 지원) |
| `7ec07f1` | **A** | 조건 블록(`#RANDOM`/`#IF`/`#SWITCH`) 분기 맥락(`BranchId`) 보존 및 라이터 분기별 출력으로 안전한 저장 지원 |
| `10e1937` | **D** | 불안정한 수동 뷰포트 컬링을 전면 제거하고 렌더링 무결성 복구 (격자선 및 노트가 항상 100% 온전하게 표시됨) |
| `ea93c56` | — | 툴바에서 불필요한 재생/정지(세모/네모) 버튼 제거하여 UI 간소화 |
| `f8756bd` | **B** | `KeySoundPlayer` 언매니지드 네이티브 헤더 포인터 직접 제어 및 `WAVE_FORMAT_EXTENSIBLE` 지원으로 키음 발음 안정화 |
| `b784684` | **F** | **🎛️ 컨트롤 패널** 추가. 레인/키음 줄을 골라 격자에서 일괄 선택·포커스, 키음 미리듣기, 번호 일괄 교체, 확인 후 일괄 삭제 (📊 통계 창은 보기 전용으로 그대로 두고, 집계 규칙은 `NoteStatsViewModel` 한 곳을 물려받습니다) |
| `b784684` | — | `LaneNoteStat` 의 `LaneId` 자리에 `lane.Header` 가 들어가던 문제 → 채널 번호와 표시 이름을 따로 들고 있습니다 |
| `b784684` | — | base-36 키음 번호 규칙이 뷰모델마다 한 벌씩 복사돼 있던 문제 → `Services/WavKey` 한 곳으로 |
| `b784684` | — | 격자의 선택 노트 강조색을 노랑 → **빨강**으로 (드래그 선택 사각형은 노랑 그대로) |
| `57940eb` | **v0.1.2** | 파형 버킷↔시각 규칙 통일 및 순간 피크 누락 수정 |
| `48d334a` | **v0.1.3** | 음원 오프셋(Audio Offset) 지원 및 `[⚡ 오프셋 자동 맞춤]` 기능 추가 |
| `cd9c26a` | **v0.1.3** | 검색/삭제/교체 창의 마디 옮겨 복사 컨트롤 너비 확장 (3자리 수 잘림 해결) |
| `7eceeee` | — | `MainWindow` 코드 비하인드를 6개 파티션으로 분할 리팩토링 |
| `74a3d93` | — | `BmsParser` 를 5개 파티션으로 분할 리팩토링 |
| `aaea4d7` | **v0.1.4** | 게임 프로파일 기반 홀드/게이트/페어리 자동 페어링 및 격자 몸통(Body) 렌더링 지원, 306개 테스트 구축 완료 |

---

## 🚫 확인해 보니 문제가 아니었던 것

점검 중 의심했으나 실제로 돌려 보니 정상이었던 것입니다. 같은 의심을 다시 하지 않도록 남깁니다.

| 의심한 것 | 결과 |
|-----------|------|
| `#PLAYER` 왕복이 한 칸씩 밀리지 않는가 (파일은 1/2/3, 콤보박스는 0부터) | **정상.** `PullHeaderFromChart` 의 `Player - 1` 과 `BmsWriter` 의 `player + 1` 이 짝을 이룹니다. 1→0→1, 2→1→2, 3→2→3 으로 세 값 모두 제자리로 돌아옵니다 |
| 데이터 줄 길이가 청크 크기로 안 나누어떨어지면 뒤가 잘리는가 | **잘리는 것은 맞지만** 규격을 벗어난 입력에서만 나옵니다(예: 9자 데이터 → 4개만 읽고 마지막 1자 버림). 우선순위가 낮습니다 |
| 빌드 경고 `MSB3277` (WindowsBase 4.0 vs 5.0) | **문제가 아닙니다.** WebView2가 끌고 오는 경고로 프로젝트당 1개씩 총 2개 납니다. `CoreWebView2` 만 쓰므로 동작에는 영향이 없습니다 |
