# BMS Editor - 확인이 필요한 알려진 문제 (Known Issues)

이 문서는 코드를 훑어 찾아낸 **의심 동작과 확정 버그**를 모아둔 것입니다.
실제로 써보면서 하나씩 확인하고, 확인이 끝난 항목은 아래 [확인 기록](#-확인-기록)에 남깁니다.

* 기준 커밋: `96e2935` (2026-08-31)
* 심각도 표기
  * 🔴 **재현 완료** — 테스트 코드로 실제 재현했습니다. 데이터가 손상됩니다.
  * 🟠 **코드상 확실** — 코드를 읽어 확인했습니다. 눌러보면 바로 드러납니다.
  * 🟡 **확인 필요** — 의심되는 동작입니다. 실사용 확인이 필요합니다.

> 1~7번은 `1b58a18`, 8~17번은 `b3aa64a`, 18번 이후는 `96e2935` 기준 점검에서 나온 것입니다.
> 8~11번과 18·19번, 26-1~26-3번은 파서/라이터를 실제로 돌려 결과를 붙였습니다.

---

## 🎯 고치는 순서 (권장 우선순위)

번호 순서가 아니라 **위험도 순서**입니다. 위쪽일수록 "사용자가 눈치채기 전에 이미 손해가 난" 것입니다.

| 순위 | 묶음 | 항목 | 왜 여기인가 |
|------|------|------|-------------|
| **1** | **파일 왕복 무결성** | 18, 8+9+19, 11+1, 16 | 열고 저장만 해도 원본이 상합니다. Undo도 백업도 없어 되돌릴 방법이 없습니다. 전부 `Services/` 안이라 UI에 안 묶여 있고 왕복 테스트 21개가 깔려 있어, **가장 위험한 걸 가장 안전하게** 고칠 수 있는 구간입니다 |
| **2** | **작업물·프로세스 손실** | 13(+26-6), 20, 21, 22 | 빈도는 낮지만 한 번에 전부 잃습니다. 20·21·22는 각각 몇 줄이라 1순위 작업 중간에 끼워 넣어도 부담이 없습니다 |
| **3** | **편집이 조용히 어긋남** | 12(+7-2), 5+6, 23, 4, 14 | 사용자 행동의 결과라 추적은 됩니다. 14번은 격자·노트·재생·라이터를 모두 건드려야 해서 이 묶음에서 가장 큽니다 |
| **4** | **있는데 동작 안 하는 UI** | 2, 3, 15, 7-1, 7-3, 7-5, 26-4, 26-5 | 데이터는 안 상하지만, 기능이 없는 것보다 있는 척하는 쪽이 더 나쁩니다. 구현하든 지우든 둘 중 하나여야 합니다 |
| **5** | **성능** | 24, 25, [참고: 성능](#-참고-성능) | 실제로 느려진 걸 확인하기 전엔 손대지 않는 편이 낫습니다. 손댄다면 뷰포트 컬링이 전부고 나머지는 곁가지입니다 |
| **6** | **정리** | [참고: 정리하면 좋을 코드](#-참고-정리하면-좋을-코드) | 언제 해도 됩니다 |

---

## 🔴 1. 3자리 키음 차트에 키음을 추가하면 기존 키음이 덮어써진다

### 증상
키가 `#001`, `#00A`, `#01Z` 처럼 **3자리**인 차트에서 `[키음 추가]`를 하고 저장하면,
새 키음이 **기존 `#001`을 덮어씁니다.** 001번을 쓰던 모든 노트가 다른 소리로 바뀝니다.
아무 경고도 뜨지 않습니다.

### 원인
* [MainWindowViewModel.cs](file:///h:/source/repos/bms%20editer/bms%20editer/ViewModels/MainWindowViewModel.cs) 의 `GetNextWavKey()` 는 **항상 2자리 키만** 만듭니다.
  기존 키가 전부 3자리(`001`~)이므로 2자리 `01` 은 "비어 있는" 키로 판정됩니다.
* [BmsWriter.cs](file:///h:/source/repos/bms%20editer/bms%20editer/Services/BmsWriter.cs) 는 3자리 차트를 만나면 `keyWidth = 3` 으로 잡고
  `PadLeft(3, '0')` 으로 폭을 맞춥니다. 그래서 `01` → `001` 이 되어 충돌합니다.
* 참고로 검색/삭제/교체 창(`NoteSearchViewModel`)은 키 폭을 제대로 다룹니다.
  즉 이건 의도된 동작이 아니라 `GetNextWavKey()` 쪽이 빠뜨린 것입니다.

### 재현 결과
```
[키음 추가] 가 만들어낼 새 키: "01"   (기존 키는 전부 3자리)

=== 저장 결과 ===
  #WAV001 snd001.wav
  #WAV002 snd002.wav
  #WAV003 snd003.wav
  #WAV001 새키음.wav        <-- 충돌

다시 읽으면 001 이 가리키는 파일: 새키음.wav
```

### 확인 방법
> ⚠️ **반드시 사본으로 하세요. 원본 차트가 손상됩니다.**

1. 3자리 키음 차트를 사본으로 연다
2. 키음 팔레트에서 `[키음 추가]` 로 아무 WAV나 넣는다
3. 저장 후 다시 연다
4. `#001` 이 원래 파일을 가리키는지 확인한다

---

## 🟠 2. "격자에 맞추기(G)" 체크박스가 아무 일도 하지 않는다

`SnapToGrid` 속성은 [MainWindow.axaml](file:///h:/source/repos/bms%20editer/bms%20editer/MainWindow.axaml) 의 체크박스에 바인딩되어 있을 뿐
**코드 어디에서도 읽히지 않습니다.** 노트 배치는 체크 상태와 무관하게 항상 격자로 반올림됩니다.

**확인 방법**: 체크를 끄고 격자 사이에 노트를 찍어봅니다. 그래도 격자에 붙으면 확정입니다.

---

## 🟠 3. "수직위치 고정(D)" 체크박스도 아무 일도 하지 않는다

`LockVerticalPosition` 역시 바인딩만 있고 소비하는 곳이 없습니다. 2번과 동일합니다.

---

## 🟠 4. 방향키로 노트를 옮기면 현재 격자로 강제 스냅된다

### 증상
`MoveNoteInTime` 이 `Math.Round((Measure + Position) * split)` 으로 위치를 다시 계산합니다.
그래서 **현재 격자로 표현할 수 없는 위치의 노트는 옮기는 순간 위치 자체가 바뀝니다.**
3잇단음처럼 12분할로 찍은 노트를 16분할 상태에서 건드리면 잇단음이 깨집니다.

### 확인 방법
1. 격자를 `12` 로 두고 3잇단음을 찍는다
2. 격자를 `16` 으로 바꾼다
3. 그 노트를 방향키로 한 칸 옮긴다 → 위치가 16분음표로 뭉개지는지 본다

---

## 🟠 5. 노트가 겹치면 저장할 때 하나가 조용히 사라진다

[BmsWriter.cs](file:///h:/source/repos/bms%20editer/bms%20editer/Services/BmsWriter.cs) 가 `slots[index] = code` 로 덮어쓰기 때문에,
같은 마디·레인·위치에 노트가 둘 있으면 **마지막 하나만 남습니다.**

겹치게 만드는 경로가 있습니다. `IsSlotOccupied` 가 **선택된 노트를 검사에서 제외**하므로,
여러 개를 선택해 이동시킬 때 일부가 막히면 남은 노트 위로 겹쳐 올라갈 수 있습니다.

---

## 🟠 6. 그룹 이동 시 일부만 움직여 모양이 깨진다

`MoveNoteInTime` 은 마디 범위를 벗어나거나 슬롯이 차 있으면 **그 노트 하나만** 조용히 건너뜁니다.
선택 전체가 취소되는 것이 아니라서 상대 간격이 무너집니다.

**확인 방법**: 노트 여러 개를 선택하고, 앞이 막힌 방향으로 방향키를 눌러봅니다.

---

## 🟡 7. 그 밖에 확인이 필요한 것들

| # | 증상 | 확인 방법 |
|---|---|---|
| 7-1 | **Delete·방향키가 격자에 포커스가 있을 때만 동작한다.** 팔레트나 사이드바를 만진 뒤에는 먹지 않고 안내도 없다 | 팔레트에서 키음을 고른 직후 Delete 를 눌러본다 |
| 7-2 | **곡 마지막 마디에 노트를 못 찍을 수 있다.** `measure >= MeasureCount` 면 배치를 거부하는데 `MeasureCount` 는 오디오 길이에서 올림 계산한다 | 곡 맨 끝 마디에 찍어본다 |
| 7-3 | **우클릭 삭제 허용오차가 배치보다 50배 넓다** (배치 `0.0001` / 삭제 `0.005`) | 격자를 192 로 두고 촘촘히 찍은 뒤 하나만 우클릭한다. 옆 노트가 같이 지워지는지 본다 |
| 7-4 | **키음이 1295개를 넘으면 `[키음 추가]` 가 조용히 실패한다.** `GetNextWavKey` 가 던진 예외를 `AddWav` 가 삼킨다 | 키음이 많은 차트에서만 재현된다 |
| 7-5 | **저장 성공 표시가 없다.** 실패만 알림창이 뜬다 | 저장을 눌러도 아무 반응이 없는 것이 정상 동작이다 |
| 7-6 | **파일 안의 주석(`*----` 줄)이 저장할 때 사라진다.** `#` 로 시작하지 않아 원문 보존 대상에서 빠진다 | 주석이 있는 차트를 열고 저장한 뒤 비교한다 |

---

## 🔴 8. Shift_JIS(일본어) 차트가 통째로 깨진다 — 키음까지 안 나온다

### 증상
야생의 BMS 차트 대부분이 Shift_JIS(CP932)인데, 열면 제목·아티스트가 뜻 모를 한글이 되고
**키음 파일명까지 깨져서 키음이 하나도 로드되지 않습니다.**

### 원인
[BmsParser.cs](file:///h:/source/repos/bms%20editer/bms%20editer/Services/BmsParser.cs) 의 인코딩 감지가
**UTF-8 실패 → CP949** 한 갈래뿐입니다. CP949는 Shift_JIS 바이트를 오류 없이 삼켜 버리기 때문에
`\uFFFD` 검사에도 안 걸리고, 그대로 엉뚱한 한글로 디코딩됩니다.

### 재현 결과
```
원본 (Shift_JIS)          파싱 결과
#TITLE 東方紅魔郷    →    '뱦뺴뛤뻷떭'
#WAV01 キック.wav    →    '긌긞긏.wav'   실제파일존재=False
#WAV02 スネア.wav    →    '긚긨귺.wav'   실제파일존재=False
```

### 고칠 방향
파일을 바이트로 한 번만 읽고 UTF-8(strict) → CP932 → CP949 순으로 시도한 뒤,
**어느 인코딩으로 읽었는지 기억해 둡니다.** 9번과 한 덩어리로 고쳐야 합니다.

---

## 🔴 9. 저장은 항상 UTF-8이라 원본 인코딩이 갈아치워진다

`SaveBms` 가 무조건 `new UTF8Encoding(false)` 로 씁니다.
CP949 한글 차트를 열어 저장하면 파일이 UTF-8로 바뀌어, 다른 플레이어·에디터에서 열면 깨집니다.
8번에서 기억해 둔 인코딩으로 되돌려 쓰면 됩니다.

---

## ✅ ~~10. 새로 만든 차트가 `#PLAYER 2`(Couple)로 저장된다~~ — 고쳐졌습니다

`_player` 기본값이 `0` 으로 바뀌어 해결됐습니다. 번호를 비워 두면 아래 항목이 밀리므로 자리만 남깁니다.
재현해 보면 새 문서가 `#PLAYER 1` 로 저장됩니다. [이미 고쳐진 것](#-이미-고쳐진-것-참고) 참고.

---

## 🔴 11. 3자리 노트 키가 잘려서 저장된다 (1번과는 다른 경로)

### 증상
노트가 3자리 키음을 가리키는데 `#WAV` 테이블에 3자리 항목이 없으면,
저장할 때 **키가 앞 두 글자만 남고 잘립니다.** 노트가 전혀 다른 소리를 가리키게 됩니다.

### 원인
`BmsWriter` 가 `keyWidth` 를 **WAV 테이블만 보고** 정합니다.
그래서 `keyWidth = 2` 가 되고, `code.Substring(0, keyWidth)` 로 노트 키가 잘립니다.

### 재현 결과
```
노트 WavKey = "0ZZ", "1AB"  /  WAV 테이블 = { "01" }

=== 저장 결과 ===
  #00011:0Z        <-- "0ZZ" 가 "0Z" 로 잘림
  #00012:001A      <-- "1AB" 가 "1A" 로 잘림
```

### 고칠 방향
`keyWidth` 를 WAV 테이블과 **노트 키 양쪽**의 최대 길이로 잡습니다.

---

## 🟠 12. OGG를 나중에 열면 뒤쪽 마디를 편집할 수 없게 된다

`UpdateMeasureCountFromAudio()` 가 `MeasureCount` 를 오디오 길이 기준으로 **무조건 덮어씁니다.**
200마디짜리 차트를 열고 그보다 짧은 OGG를 얹으면 `MeasureCount` 가 75로 줄고,
75마디 이후 노트는 **화면에는 보이지만** 배치·이동·복사가 전부 거부됩니다.
BPM을 만질 때마다 다시 계산되므로 BPM 조율 중에도 튀어나옵니다.

`Math.Max(오디오 기준, 차트 기준)` 이어야 합니다. 7-2번과 뿌리가 같습니다.

**확인 방법**: 마디가 많은 차트를 열고, 그보다 짧은 OGG를 얹은 뒤 뒤쪽 마디에 노트를 찍어봅니다.

---

## 🟠 13. 창을 X로 닫으면 저장 안 한 작업이 경고 없이 사라진다

새로 만들기·열기·폴더 열기에는 `ConfirmDiscardIfNeededAsync` 확인창이 있는데,
정작 **창 닫기에는 없습니다.** `Closing` 핸들러가 코드 전체에 하나도 없습니다.

곁들여, 지금은 "고친 내용이 있는지"(dirty)를 추적하지 않고 `HasDocumentContent`(내용이 있는지)로만
판단하므로, 아무것도 안 고쳤는데도 확인창이 뜹니다.

---

## 🟠 14. `#xxx02`(박자 변경)를 격자가 무시한다

`#xxx02` 줄은 원문 보존 대상이라 저장하면 되돌아오지만,
`BmsChart.MeasureLengths` 를 **채우는 코드가 아예 없습니다**(`GetMeasureLength` 도 호출처 0건).
그래서 4/4가 아닌 마디가 섞인 차트는 격자와 노트가 오디오와 어긋납니다.

**확인 방법**: `#00002:0.75` 같은 줄이 있는 차트를 열고, 그 마디 이후 격자가 소리와 맞는지 봅니다.

---

## 🟠 15. 검색창의 "롱" / "숨기기" 필터는 절대 걸리지 않는다

`BmsParser` 는 편집 대상 채널(11~18)만 읽고 **모든 노트를 `NoteType.Normal` 로만** 만듭니다.
롱노트(51~59)·숨김(31~39) 채널은 원문 보존으로 빠지므로,
`NoteSearchViewModel` 의 `IncludeLong` · `IncludeHidden` 은 어떤 노트에도 해당되지 않습니다.
즉 화면에는 있는데 아무 효과가 없는 버튼입니다. `NoteType.Mine` 도 만들어지는 곳이 없습니다.

---

## 🟠 16. 저장할 때 `#WAV` 경로가 추측 결과로 덮어써진다

파일이 없으면 하위 폴더에서 동명 파일을 찾아 대신 쓰는 보강 기능(README 8번)이 있는데,
그 **추측 결과가 저장 파일에 그대로 박힙니다.** 원문은 되돌아오지 않습니다.

### 재현 결과
```
원본:        #WAV01 kick.wav          (실제 파일 없음, old_backup/ 에 동명 파일 있음)
저장 결과:   #WAV01 old_backup\kick.wav
```

오래된 백업 폴더가 남아 있으면 그쪽을 가리키도록 차트가 바뀝니다.
재생용으로만 쓰고 저장은 원문 문자열을 유지하는 편이 안전합니다.

> 참고로 파일이 정상적으로 있는 보통의 경우는 `sounds/kick.wav` → `sounds\kick.wav` 로
> 구분자만 바뀌어 제대로 왕복합니다. 문제는 **못 찾았을 때**뿐입니다.

---

## 🟠 17. 디코더가 `TotalSamples` 보다 많이 내보내면 OGG 로드가 통째로 실패한다

`OggAudioPlayer.DecodePcm16` 이 `TotalSamples` 크기로 버퍼를 잡고 `bytes[byteOffset++]` 로 그냥 씁니다.
granule 값이 어긋난 파일에서 디코더가 조금 더 내보내면 `IndexOutOfRangeException` 이 나고,
`LoadOgg` 가 삼켜서 "로드 실패"로만 표시됩니다.

`OggPeakLoader` 는 같은 상황을 "디코더가 TotalSamples보다 조금 더 내보내도 마지막 버킷에 흡수시킨다"로
명시적으로 처리하고 있습니다. 이쪽만 빠졌습니다.

---

## 🔴 18. `#RANDOM`/`#IF` 차트가 저장하면 통째로 무너진다

### 증상
`#RANDOM` 으로 갈래를 나눈 차트를 열어서 **저장만 해도** 조건 블록이 껍데기만 남고,
갈래별로 달랐던 노트가 **하나로 합쳐져 항상 동시에 나오는 패턴**이 됩니다.

### 원인
두 겹입니다.

* `#RANDOM`·`#IF`·`#ENDIF` 는 데이터 줄(`#mmmCC:`)이 아니라서 **"헤더 성격의 보존줄"** 로 분류되고,
  [BmsWriter.cs](file:///h:/source/repos/bms%20editer/bms%20editer/Services/BmsWriter.cs) 가 그런 줄을 전부 **파일 맨 위 헤더 블록으로 끌어올립니다.**
  보존줄이 원래 파일에서 몇 번째 줄이었는지를 기억하지 않기 때문입니다.
* 그 사이에 있던 건반 줄은 조건 맥락을 잃고 **평범한 노트로 읽힙니다.** 저장할 때는 같은 마디·레인이라
  한 줄로 병합됩니다.

### 재현 결과
```
=== 원본 ===                    === 저장 결과 (헤더 생략) ===
#00101:01                       #RANDOM 2
#RANDOM 2                       #IF 1
#IF 1                           #ENDIF          <-- 속이 빈 껍데기
#00111:01000000                 #IF 2
#ENDIF                          #ENDIF          <-- 속이 빈 껍데기
#IF 2                           #WAV01 a.wav
#00111:00001000                 #00101:01
#ENDIF                          #00111:0110     <-- 두 갈래가 하나로 합쳐짐
```

### 고칠 방향
* **당장**: 파싱할 때 `#RANDOM`/`#IF`/`#SWITCH` 가 보이면 **저장을 막고 경고**합니다. 이것만으로 피해가 0이 됩니다.
* **제대로**: `BmsRawLine` 에 원본 줄 번호를 달아 보존줄이 제자리를 지키게 합니다.
  26-3번(마디 1000 이상 줄이 헤더로 올라가는 것)도 같은 수정으로 사라집니다.

### 확인 방법
> ⚠️ **반드시 사본으로 하세요.**

`#RANDOM` 이 들어간 차트를 열고 아무것도 고치지 않은 채 저장한 뒤 원본과 비교합니다.

---

## 🔴 19. 인코딩 감지가 앞 100줄만 봐서, CP949 차트도 깨질 수 있다

### 증상
8번과 별개의 구멍입니다. **지원한다고 되어 있는 CP949 차트조차** 비ASCII 글자가
파일 앞쪽 100줄 밖에 있으면 감지에 실패해, 그 글자가 복구 불가능한 대체 문자(`U+FFFD`)로 바뀝니다.

키음이 수백 개인 차트에서 헤더는 영문이고 `#WAV` 파일명만 한글/일본어인 경우가 여기에 딱 걸립니다.

### 원인
[BmsParser.cs](file:///h:/source/repos/bms%20editer/bms%20editer/Services/BmsParser.cs) 의 `IsMalformedUtf8` 이
`Math.Min(lines.Length, 100)` 으로 앞 100줄만 검사합니다. 그 뒤에서 처음 깨지면 CP949 재시도가 아예 돌지 않습니다.

### 재현 결과
```
원본(CP949): ASCII #WAV 줄 120개 뒤에  #TITLE 한글제목입니다

읽어낸 제목: [�ѱ������Դϴ�]      <-- 대체 문자라 되돌릴 수 없음
```

### 고칠 방향
8·9번과 **반드시 한 덩어리로** 고칩니다. 파일을 바이트로 한 번 읽어
strict UTF-8(`DecoderExceptionFallback`) → CP932 → CP949 순으로 시도하면
줄 수 제한 자체가 필요 없어지고, 그때 기억해 둔 인코딩으로 9번(저장)까지 해결됩니다.

---

## 🟠 20. 저장이 원자적이지 않아, 실패하면 원본이 잘린 채 남는다

`SaveBms` 가 `File.WriteAllText` 로 **원본을 바로 덮어씁니다.**
쓰는 도중에 디스크가 차거나 앱이 죽으면 원본 차트가 잘린 상태로 남고, 되돌릴 사본이 없습니다.

임시 파일에 쓴 뒤 `File.Replace` 로 바꿔치면 해결되고, `.bak` 도 함께 얻습니다.
**비용 대비 효과가 이 문서에서 가장 좋은 항목입니다.**

---

## 🟠 21. 재생 중 예외가 앱을 그대로 죽인다

[OggAudioPlayer.cs](file:///h:/source/repos/bms%20editer/bms%20editer/Services/OggAudioPlayer.cs) 의
`ThrowIfFailed(WaveOutOpen(...))` 이 던지는 예외를
[MainWindowViewModel.cs](file:///h:/source/repos/bms%20editer/bms%20editer/ViewModels/MainWindowViewModel.cs) 의
`PlayFrom` 이 감싸지 않습니다. `LoadOgg` 에는 try/catch 가 있는데 **재생 경로만 비어 있습니다.**

오디오 장치가 없거나 다른 앱에 잡혀 있으면 재생 버튼 한 번에 크래시하고, 편집 중이던 내용이 전부 사라집니다.

**확인 방법**: 오디오 장치를 독점하는 앱을 띄워 둔 채 재생을 눌러봅니다.

---

## 🟠 22. OGG 로드에 실패하면 잘 물려 있던 음원까지 지워진다

`LoadOgg` 의 catch 가 `_audioPlayer`(= **기존** 플레이어)를 Dispose 하고
`OggDurationSeconds`·`OggPeaks`·`OggOnsets` 를 0/null 로 밀어버립니다.

새로 고른 파일이 깨졌을 뿐인데 **멀쩡하던 파형과 재생이 같이 사라집니다.**
실패했을 때는 기존 상태를 그대로 두고 메시지만 보여주면 됩니다.

---

## 🟠 23. 키음을 지워도 그 번호를 쓰던 노트가 그대로 남는다

`RemoveWav` 가 `WavTable` 과 `WavList` 에서만 지우고 노트는 건드리지 않습니다.
남은 노트는 **소리가 나지 않는 유령 노트**가 되고, 저장하면 `#WAV` 정의가 없는 번호를 가리키는 파일이 나옵니다.
통계 창에 `(등록되지 않은 번호)` 로 뜨는 것이 유일한 단서입니다.

지우기 전에 "이 번호를 쓰는 노트 N개가 있습니다" 확인을 받아야 합니다.

---

## 🟠 24. 스크러빙 중 마우스를 움직일 때마다 오디오 장치를 다시 연다

가운데 버튼 드래그는 `PointerMoved` → `ScrubToRatio` → `PlayFrom` 으로 이어지는데, `PlayFrom` 이 매번

1. `Chart.Notes.OrderBy(...).ToArray()` — 노트 **전체 정렬 + 배열 복사**
2. `waveOutReset` → `waveOutUnprepareHeader` → `waveOutClose` → `waveOutOpen` → `GCHandle.Alloc(pinned)` → `Write`

를 전부 수행합니다. 초당 100회 넘게 들어오는 이벤트라, 노트가 많으면 끊기고 장치를 계속 여닫아 딸깍거리기 쉽습니다.

드래그 중에는 위치만 갱신하고 **버튼을 뗄 때 한 번만** 재생하거나, 최소한 정렬 결과를 캐시해야 합니다.

---

## 🟠 25. 비디오 프리뷰에 세 가지가 겹쳐 있다

[VideoPreviewControl.cs](file:///h:/source/repos/bms%20editer/bms%20editer/Views/Controls/VideoPreviewControl.cs)

| # | 내용 |
|---|---|
| 25-1 | **33ms마다 WebView2로 스크립트를 보냅니다.** `PlaybackPositionSeconds` 가 바뀔 때마다 `SyncTo` → `ExecuteScriptAsync` 라 재생 중 초당 30번 IPC 왕복이 돕니다. 오차 검사는 JS 안에서 하므로 **보낼 필요 없는 호출도 전부 나갑니다.** 0.2초에 한 번 정도로 줄이면 됩니다 |
| 25-2 | **네이티브 창이라 ScrollViewer에 잘리지 않습니다.** `_controller.Bounds` 를 좌표만 보고 잡는데, 오른쪽 패널을 스크롤하면 원점이 패널 밖으로 나갑니다. `ClipToBounds` 는 네이티브 HWND에 안 먹으므로 툴바 위로 비디오가 삐져나옵니다 (🟡 실물 확인 필요) |
| 25-3 | **초기화 도중에 떼어내면 컨트롤러가 샙니다.** `Dispose()` 가 먼저 돌고 뒤늦게 `CreateCoreWebView2ControllerAsync` 가 완료되면 `_controller` 에 살아 있는 컨트롤러가 대입되고 아무도 `Close()` 하지 않습니다. WebView2 프로세스가 남습니다 |

---

## 🟡 26. 그 밖에 확인이 필요한 것들 (2차)

| # | 증상 | 비고 |
|---|---|---|
| 26-1 | **중복 `#WAV01` 정의가 저장하면 두 줄로 늘어난다.** `WavTable` 은 마지막 것만 남는데 `WavItems` 에는 둘 다 들어간다 | 재현 완료 |
| 26-2 | **원본에 없던 빈 헤더가 생긴다.** 3줄짜리 차트가 `#TITLE `·`#GENRE `·`#PLAYLEVEL ` 같은 빈 값을 달고 11줄이 된다 | 재현 완료 |
| 26-3 | **마디 1000 이상 데이터 줄이 헤더 블록으로 올라간다.** `DataRegex` 가 마디를 3자리로만 받아 해석에 실패하고, 18번과 같은 경로로 맨 위로 끌려간다 | 재현 완료 · 규격 밖이라 우선순위 낮음 |
| 26-4 | **검색창의 키 자릿수가 "창을 연 시점"에 고정된다.** 창을 띄워둔 채 3자리 차트를 열면 번호 범위 기본값이 2자리로 남는다 | `NoteSearchViewModel._keyWidth` |
| 26-5 | **곡 제목에 `:` `?` `/` 가 있으면 저장 대화상자가 걸린다.** `SuggestedFileName = vm.Title` 을 그대로 넘긴다 | `PickSavePathAsync` |
| 26-6 | **`ConfirmWindow` 가 확인/취소 2지선다뿐이다.** 13번(창 닫기 확인)을 제대로 하려면 **저장 / 저장 안 함 / 취소** 3지선다가 먼저 필요하다 | 13번의 선행 작업 |
| 26-7 | **선택에 Ctrl/Shift 수정자가 없다.** 드래그할 때마다 이전 선택이 통째로 날아가서 떨어진 구간을 함께 고를 수 없다 | `NoteGridControl.OnPointerReleased` |

---

## 📉 참고: 성능

노트가 수천 개인 차트, 또는 분할을 크게 둔 상태에서 체감될 수 있는 것들입니다.

| 항목 | 내용 |
|------|------|
| **뷰포트 컬링 없음** | `NoteGridControl` · `OggWaveformControl` 이 매 프레임 타임라인 **전체**를 그립니다. 5분·BPM 150·16분할이면 격자선만 약 3,000개 × 2컨트롤, 192분할이면 약 36,000개 × 2. 재생 타이머가 33ms마다 `PlaybackPositionSeconds` 를 바꾸므로 초당 30번 반복됩니다. 가장 크게 남는 개선 여지입니다 |
| **렌더마다 Pen/Brush 새로 생성** *(절반 해결)* | 노트 테두리 펜과 선택 강조 펜은 `static readonly` 로 올라갔습니다. 남은 것: `NoteGridControl` 이 **노트 하나마다** 채움 `SolidColorBrush` 를, `OggWaveformControl.DrawOnsetMarkers` 가 **온셋 마커(최대 20,000개)마다** `Pen` + `SolidColorBrush` 를 여전히 새로 만듭니다 |
| **재생/스크러빙 경로** | 24번(스크러빙 중 장치 재개방 + 노트 전체 정렬), 25-1번(초당 30회 WebView2 IPC) 참고 |
| **`Notes` 프로퍼티가 매번 배열 복사** | `MainWindowViewModel.Notes` 가 `Chart.Notes.ToArray()`, `SelectedNotes` 가 `_selectedNotes.ToArray()` 입니다 |
| **노트 조회가 선형 탐색** | `PlaceNote` 의 `Find`, `IsSlotOccupied` 의 `Any` 가 O(n). 여러 개 선택 이동은 O(n×m)이 됩니다 |
| **통계 창이 클릭마다 전체 재집계** | `NoteStatsViewModel.Refresh()` 가 `Notes` 변경마다 (레인 수 × 노트 수) 순회 + `GroupBy` 를 다시 돕니다. 통계 창을 띄워둔 채 편집하면 노트 하나 찍을 때마다 실행됩니다 |

---

## 🧹 참고: 정리하면 좋을 코드

| 항목 | 위치 |
|------|------|
| **타임라인 길이 공식 중복** — `MainWindow.GetTimelineLength()` 와 `TimelineControlBase.GetTimelineHeight()` 가 같은 계산을 따로 합니다. 이미 `Math.Max(1.0, …)` 유무로 미세하게 갈라져 있어, 한쪽만 고치면 스크롤 추적이 어긋납니다 | MainWindow.axaml.cs / TimelineControlBase.cs |
| **중복 삭제 루프** — `DeleteSelectedNotes` 가 바로 옆에 있는 `DeleteNotes` 를 쓰지 않고 같은 루프를 또 갖고 있습니다. `3a026fe` 중복 정리 때 남은 자리입니다 | MainWindowViewModel.cs |
| **필드 이름과 값이 어긋남** — `LaneNoteStat(LaneId, …)` 자리에 `lane.Header` 를 넣습니다. 지금은 Id와 Header가 같아 안 드러나지만 레인 구성을 바꾸면 어긋납니다 | NoteStatsViewModel.cs |
| **`BmsChart` 가 `INotifyPropertyChanged` 가 아님** — 그런데 XAML이 `{Binding Chart.Lanes}` 로 묶여 있습니다. 레인 구성을 바꾸는 기능이 생기는 순간 격자가 갱신되지 않는 함정이 됩니다 | BmsChart.cs / MainWindow.axaml |
| 안 쓰이는 멤버 — `BmsRawLine.Channel`(설정만 하고 읽는 곳 없음), `BmsChart.BmpTable`(채우는 곳 없음), `NoteType.Mine` | Models |
| 스타일 불일치 — `MainWindow.axaml.cs` 만 멤버 들여쓰기가 8칸, `Program.cs` · `App.axaml.cs` 만 블록 네임스페이스 | — |
| 메뉴 `편집 (E)` · `View` · `설정 (O)` · `보기 (C)` · `프리뷰 (P)` 가 빈 껍데기 | MainWindow.axaml |
| `.gitignore` 의 `[Re]leases/` 오타 (`[Rr]eleases/` 의도로 보임) | .gitignore |

빌드 경고는 WebView2가 끌고 오는 `MSB3277`(WindowsBase 4.0 vs 5.0) 뿐입니다(프로젝트당 1개, 총 2개).
`CoreWebView2` 만 쓰므로 동작에는 영향이 없습니다.

---

## 🧩 참고: 빠진 기능

버그는 아니지만 에디터로서 비어 있는 자리입니다.

* **Undo / Redo 가 없습니다.** 가장 큰 빈 자리입니다.
* **단축키가 하나도 없습니다.** Ctrl+S · Ctrl+O · Ctrl+N 모두 미연결 (`KeyGesture` 검색 결과 0건).
* **제목 표시줄에 파일명·수정 표시가 없습니다.** 항상 `bms editer` 로만 뜹니다.
* **노트에 키음 번호가 표시되지 않습니다.** `NoteGridControl` 의 `DrawText` 는 레인 머리글 3곳뿐이고,
  노트는 색 채운 네모/동그라미로만 그립니다. 어떤 키음이 찍혔는지는 클릭해 보기 전에는 알 수 없습니다.
* **레인 구성이 `16,11,12,13,14,15,18` 고정입니다.** `Chart.Lanes` 에 setter 는 있으나 아무도 쓰지 않아
  5키·10키·14키·Double 차트는 1P 쪽만 보입니다.
  (채널 `19` 를 편집 대상에서 뺀 것은 **의도된 선택**입니다. `19` 줄은 보존줄로 왕복하므로 저장해도 살아남습니다.)
* **선택에 Ctrl/Shift 수정자가 없습니다.** 26-7번 참고.

---

## ✅ 확인 기록

확인이 끝나면 결과와 날짜를 적습니다.

| # | 항목 | 결과 | 확인일 | 메모 |
|---|------|------|--------|------|
| 1 | 3자리 키음 추가 시 덮어쓰기 | | | |
| 2 | 격자에 맞추기 체크박스 | | | |
| 3 | 수직위치 고정 체크박스 | | | |
| 4 | 방향키 이동 시 격자 스냅 | | | |
| 5 | 노트 겹침 시 저장 유실 | | | |
| 6 | 그룹 이동 시 모양 깨짐 | | | |
| 7-1 | 포커스에 따른 Delete·방향키 | | | |
| 7-2 | 마지막 마디 배치 | | | |
| 7-3 | 우클릭 삭제 허용오차 | | | |
| 7-4 | 키음 개수 한계 | | | |
| 7-5 | 저장 성공 표시 | | | |
| 7-6 | 주석 줄 유실 | | | |
| 8 | Shift_JIS 차트 깨짐 (키음 포함) | | | |
| 9 | 저장 시 인코딩 UTF-8 고정 | | | |
| 11 | 3자리 노트 키 잘림 | | | |
| 12 | OGG 로드 후 뒤쪽 마디 편집 불가 | | | |
| 13 | 창 닫기 시 경고 없음 | | | |
| 14 | `#xxx02` 박자 변경 무시 | | | |
| 15 | 롱·숨기기 필터 무효 | | | |
| 16 | 저장 시 `#WAV` 경로 덮어쓰기 | | | |
| 17 | OGG 디코딩 버퍼 초과 실패 | | | |
| 18 | `#RANDOM`/`#IF` 블록 붕괴 | | | |
| 19 | 앞 100줄 밖 비ASCII 인코딩 감지 실패 | | | |
| 20 | 저장이 원자적이지 않음 | | | |
| 21 | 재생 예외로 앱 종료 | | | |
| 22 | OGG 로드 실패가 기존 음원까지 삭제 | | | |
| 23 | 키음 삭제 후 유령 노트 | | | |
| 24 | 스크러빙 중 장치 재개방 | | | |
| 25-1 | 비디오 IPC 초당 30회 | | | |
| 25-2 | 비디오가 ScrollViewer 밖으로 삐져나옴 | | | |
| 25-3 | 비디오 초기화 경합으로 컨트롤러 누수 | | | |
| 26-1 | 중복 `#WAV` 가 두 줄로 저장 | | | |
| 26-2 | 원본에 없던 빈 헤더 생성 | | | |
| 26-3 | 마디 1000 이상 줄이 헤더로 이동 | | | |
| 26-4 | 검색창 키 자릿수 고정 | | | |
| 26-5 | 곡 제목의 파일명 금지문자 | | | |
| 26-6 | `ConfirmWindow` 2지선다 한계 | | | |
| 26-7 | Ctrl/Shift 다중 선택 없음 | | | |

---

## 🗒️ 이미 고쳐진 것 (참고)

아래는 같은 점검에서 나왔으나 이미 수정을 마친 항목입니다. 다시 확인할 필요는 없습니다.

| 커밋 | 내용 |
|------|------|
| `f86a58c` | 파형 피크 버킷 절삭으로 시간축이 누적해서 어긋나던 문제 (2분 지점 53ms) |
| `15a867f` | 저장할 때 BGM·마디 길이·BPM 변화·확장 헤더 등이 사라지던 문제 (36줄 중 23줄 소실 → 1줄) |
| `e19c25b` | 재생 커서가 벽시계 기준이라 실제 소리보다 앞서 나가던 문제 (약 12ms + 드리프트) |
| `80c26b6` | **"테스트 프로젝트 없음"** — 파서·라이터 왕복과 격자선 계산 테스트가 생겼습니다 (현재 21개 통과) |
| `3a026fe` | **10번** 새 문서가 `#PLAYER 2`(Couple)로 저장되던 문제 (`_player` 기본값 `1` → `0`). 재현해 보면 이제 `#PLAYER 1` 이 나옵니다 |
| `3a026fe` | 격자선 계산이 네 벌로 흩어져 있던 문제 → `TimelineControlBase.EnumerateGridLines` 한 곳으로. 같이 사라진 것: `goto FinishedBeatLines`, `DrawBeatGrid` 의 죽은 대입(`tPos += rowHeight`) |
| `3a026fe` | 보조 창들이 구독 해제 절차를 각자 복사해 갖고 있던 문제 → `OwnerObservingViewModel` 로. 노트 테두리·선택 강조 펜도 `static readonly` 로 올라갔습니다 |
